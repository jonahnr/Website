# Scorecard submissions

This folder is for exported Dashboard Trust Scorecard submissions.

Because the marketing site is static, browser submissions cannot write directly into this repository at runtime. The hosted form sends email through FormSubmit, and the page also stores a browser-side CSV backup that can be downloaded from the thank-you or scorecard pages and archived here.
